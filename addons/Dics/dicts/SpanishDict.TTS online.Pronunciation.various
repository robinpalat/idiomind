#!/bin/bash
# -*- ENCODING: UTF-8 -*-

if [[ "$1" = dlgcnfg ]]; then
source "$DS/ifs/mods/cmns.sh"
msg "$(gettext "Does not need configuration")\n" info "$4"

else
if [ "$lgt" = en ]; then t=en
elif [ "$lgt" = es ]; then t=es
else continue; fi
local LINK="http://audio1.spanishdict.com/audio?lang=$t&text=${word}"
local ex='mp3'
fi


