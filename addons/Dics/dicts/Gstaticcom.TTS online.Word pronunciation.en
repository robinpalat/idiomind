#!/bin/bash

if [[ "$1" = dlgcnfg ]]; then
source "$DS/ifs/mods/cmns.sh"
msg "$(gettext "Does not need configuration.")\n" info "$4"


else
name="gstatic"
lang="en"
wget -T 51 -q -U Mozilla "http://ssl.gstatic.com/dictionary/static/sounds/de/0/${1}.mp3"
exit
fi

